import { trans, getLanguages, setCurrentLocale, getCurrentLocale, registerLang, getLocaleText, format, registerFormatters, getFormatters, process } from 'ts-easy-i18n';
import Text from './Text';

export {
    getLanguages,
    getCurrentLocale,
    setCurrentLocale,
    registerLang,
    getLocaleText,

    trans,

    format,
    getFormatters,
    registerFormatters,

    process,
    Text
};
